[package]
name = "lucille"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Cherrypic's Lucene-like search query parser"

[lib]
crate-type = ["rlib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
regex = "1"
once_cell = "1"
